# Site Shelf

One-page tool for keeping live previews of sites you built. Links are stored in the browser on the device where you add them.

Run it locally:

```bash
npm install
npm run dev
```

Some websites block embedded previews. The open button on each card always opens the saved site in a new tab.
