TODO
- do u see the new KC with bootstrap5? can you make david-ui the same exact coding style. as many classes like bootstrap ones or row or col or col-md-4 etc 
DONE
- i want the API and UI in diff repos git that are unseeable from SAS
	Created separate local `david-api` and `david-ui` Git repositories and removed both from SAS tracking.
	-
- using the exact same coding structure as KC, I want a website for displaying my websites. The API can be David – API and the UI can be David – UI. This site will have two main functions. One will be displaying the websites. I've already built where I will simply provide a link to those website websites and maybe it will be some sort of i frame. The second function will be a form for requesting a commissioned website. This will have some similarities to KC. But since it is a website, it will have different options obviously
	Built `david-api` and `david-ui` with screenshot portfolio cards, private website-request pages, email links, and environment-configured admin tools.
- when building websites for other people, how would I hand it off to them without having the burden of server maintenance? Or would that be something I should offer or is there a way to hand it off because if I build 20 websites I wouldn't want to do server maintenance for 20 different websites and monitor their health, etc..
	Default to client-owned hosting: the client owns and pays for the domain, DNS, deployment account, database/storage, email, and payment accounts; you are added as a developer while building. Deliver the source repository to their organization, transfer the deployment, document environment variables and routine updates, then remove your access after a defined handoff window. For ordinary static sites, managed hosting is usually enough; for dynamic sites, use a client-owned managed platform and database rather than a server you personally maintain. Offer ongoing maintenance only as a separate monthly service with a written scope, response times, backups, and update fees. That makes 20 completed client sites 20 client-owned services, not 20 servers you are quietly on call for.
- route 53 for domain... do i need to route traffic to my IP for port forwarding thing?
	Yes. Point Route 53 `A` records for the domain and `www` at the router's public IPv4 address; the router then forwards TCP 80 and 443 to the home server. DNS has no port field, so do not use the server's private `192.168.x.x` address. This needs the static public IP answered in `0_QUESTIONS`, or a dynamic-DNS updater.
- why doesn the scheduler .service need to specify a port or anything
	It is a background worker, not a web server. It starts `run_scheduler.py`, waits for timed jobs, and talks only to the database, email provider, and Stripe API through its environment; no browser or Nginx request connects to it. Only the Gunicorn API service needs port 8001 because Nginx forwards web requests there.
- wont i need to do port forwarding on my xfinity router or whatever for the hosting.md? if so add instructions for that too
	Added current Xfinity app steps for TCP ports 80 and 443, plus the private ports that must remain closed.
- add the simplest way to setup postgresql DB on my hosting.md thing. will be using that for DB. also consolidate all sql scripts in sas-api into one startup script and remove any unnecessary commands and consolidate simpler
	Added PostgreSQL setup and a single idempotent SAS startup script, removing superseded SQL fragments and role-setup helpers.
- why are they queued and not sent right away for those? still confused
	They were queued so web requests do not wait for SMTP and all mail sends from the scheduler. That creates the current 15-minute delay; password resets, confirmations, and waitlist notices are better sent immediately, while reminders can stay queued.
- what queue emails are you talking about?
	Password resets, registration confirmations, waitlist spot-open notices, and event reminders are queued in the database and sent every 15 minutes.
- SAS Scheduler Footprint: Explain the scheduler memory tradeoff and the lighter timer alternative.
	It also processes queued email every 15 minutes. A `systemd` timer could start a short command only when needed, but the current single worker is the simpler reliable choice unless the server is low on RAM.
- brother what the heck are you doing. delete everything you just did
	Removed the Site Shelf app and its feature entry.
- using the coding style of KC as a guide, i want this DAVID app to let me add a link to any site i built and it shows a preview on the screen. make this a one page website
	Removed at your request.
- does having multiple sites on a server make it slower? or does the reverse proxy send requests to workers independently
	Documented shared-resource limits, separate worker routing, and corrected SAS to use its own port `8001`.
- do your instructions allow SSH from a different network or just if you're in the same network?
	The guide forwards only web ports, so SSH is LAN-only; it now recommends a private VPN for remote SSH.
- where are the instructions on creating the SSH key instead of PW login? and do we need this? if so why?
	Added client-side SSH key setup, a second-session check, and an explanation before disabling passwords.
- Literal Hosting Commands: Replace command variables with direct KC and SAS paths, users, and repository URLs.
	The hosting guide now uses direct commands; only domains and secrets need manual replacement.
- Hosting User Choice: Explain temporary exports and update hosting to use the existing `agentbot` account.
	Exports are terminal-only command shortcuts; the guide now runs each app under `agentbot` instead of creating service users.
- Exact Hosting Commands: Convert the hosting guide to Markdown and list the required Bash commands in deployment order.
	Added `hosting.md` with direct commands for KC, SAS, Nginx, HTTPS, and the SAS scheduler.
- Direct Server Hosting: Replace the Docker and Caddy guide with a simple Nginx plus Gunicorn or Uvicorn setup.
	The guide now uses Nginx, systemd, Gunicorn for Flask, and Uvicorn for FastAPI.
- Hosting Stack Comparison: Explain why the guide uses Docker and Caddy instead of Nginx and Gunicorn.
	Docker is for isolation and repeatable deployment, not speed; Nginx plus Gunicorn is a good lean WSGI option.
- Home Server Hosting: Create a step-by-step guide for hosting a web server from a home internet connection.
	Added an Ubuntu, Docker, Caddy, router, DNS, HTTPS, and maintenance guide in `hosting`.
- KC Style Reference: Review KC's frontend, backend, and CSS, then document its reusable conventions in `STYLE`.
	Added the product structure, palette, layout, controls, and interaction rules.
- another test, say Hi
	Hi. 
- just testing if this works. say hi
	Hi.
