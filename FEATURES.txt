- Empty Run Retention: Scheduler monitoring now prunes successful zero-change job runs after 10 days so the tracking table does not grow forever.
- Route Error Logging: Live backend event and auth routes now report unexpected exceptions through structured app logger calls instead of raw `print(...)`.
- Attendee Email Guard: Organizer and admin attendee edits now reject duplicate email collisions with a clean validation error before the database constraint fails.
- Schedule Export Cleanup: The all-schedules manager dialog no longer shows a dead save button, organizers can export it too, and the table/CSV are reduced to the core pairing columns.
- Refund Transfer Control: Connected-organizer refunds now explicitly reverse the transfer and refund the application fee so Stripe-side refund money movement is deterministic.
- Billing ID Hiding: Organizer and admin billing UI now shows Stripe connection state without exposing the raw connected account ID.
- Event Status Editing: The event edit dialog no longer allows manual status jumps into in-progress, completed, or cancelled states.
- Rebuild Script Removal: The old backend rebuild helper that recreated tables with `db.create_all()` has been removed so schema changes stay on the explicit SQL-script path.
- Manual Checkin Flow: Organizer and admin manual attendee check-in is now handled from the registered-users view, with attendee PIN self-check-in removed from the web flow.
- Church Name Cleanup: Custom church names are now normalized and sanitized before being stored or reused.
- Selection Lock Cleanup: Completed-event selection submission now locks correctly to `In Progress` only, and stale post-event grace-period gating was removed.
- Immediate Match Reveal: Attendee match access APIs now align with immediate reveal after event completion, and organizer/admin match visibility is blocked.
- Privacy Age Alignment: Privacy policy age language now matches the real `18+` sign-up rule.
- Browser Title Reset: Browser tab title is now simply `Saved & Single`.
- Profile Editing Page: Authenticated profile page lets users update their core account details and church information.
- Username Profile Access: Profile is now opened from the top-right username, and the form stays read-only until the header pencil icon enables editing.
- Repeat Match Filtering: Schedule generation now filters out attendees who have already speed-dated each other in past events.
- Readonly Profile Polish: Profile read-only mode now uses polished static detail cards, with outlined edit/cancel controls and logout moved onto the profile card.
- Birthday Timezone Stability: Birthday handling now preserves the selected calendar date across timezones in signup, profile, and attendee management views.
- Morning Event Completion: Events now auto-complete from a single 9:00 AM Eastern maintenance run, with the manual End control still removed from the web UI.
- UTC Event Conversion: Event create/edit flows continue converting browser-local datetime input to UTC for storage without persisting per-event timezone metadata.
- Header Save Controls: Profile editing now uses header save/cancel icons, with save disabled until an actual change is made.
- Cron Completion Script: A cron-friendly backend maintenance script can now auto-complete due events without waiting for user traffic.
- Flowchart Render Automation: The flowchart cron flow can now regenerate committed SVG images directly from the Mermaid charts in `flowchart.md`.
- Stripe Signup Enforcement: Paid event signups are now enforced through Stripe Checkout on the backend as well as the web UI, so direct registration calls cannot bypass payment.
- Separate Cron Pushes: The cron runner now commits and pushes `sas-api`, `sas-ui`, and the root repo separately when changes are present.
- Organizer Export Access: Organizer-created events can now export the registered-attendees CSV from the web UI, matching their existing waitlist and schedule export abilities.
- Timer Break Settings: Timer settings now let organizers/admins edit break duration in the same dialog as round duration.
- Attendee Edit Cleanup: Registered-attendee backend edits are now limited to the fields still exposed in the UI, and the unused waitlist-user update API has been removed.
- Delete Rule Alignment: Backend event-deletion rules now match the UI, so started or completed events cannot be deleted through the API either.
- Readme Flowchart Display: Root README now displays the rendered app and matching flowchart SVGs, with `flowchart.md` kept as the source Mermaid file for GitHub/mobile viewing.
- Waitlist Notify Flow: Waitlist management no longer promotes users directly into registration; open spots now keep the waitlist intact and send signup reminder emails instead.
- Landing Signin Cue: Signed-out users now see a callout bubble that points toward sign-in and explains they can attend or organize events after logging in.
- Event Balance Toggle: Event create and edit flows now include a default-on checkbox for enforcing the 60/40 gender-balance registration rule per event.
- Timer Transport Controls: Organizer and admin timer controls now use previous, play/pause, and forward transport buttons instead of the old floating text-button bar.
- Long Session Tokens: JWT access tokens now default to a one-year lifetime so the existing persistent-login flow can keep infrequent users signed in longer.
- Token Expiry Alignment: Signup and signin now issue JWTs through the shared app-wide expiry configuration instead of hardcoding a separate one-day lifetime.
- Runtime API Config: The frontend no longer forces localhost API URLs at boot and now respects runtime, build-time, or local override API base configuration.
- Embedded Event Scheduler: The Flask backend now starts an in-app 9:00 AM Eastern scheduler for event auto-completion so production no longer depends on a separately configured server cron.
- Attendee Email Reminders: Event signup now sends a registration confirmation email, and nightly 9:00 PM Eastern jobs send one-month, one-week, and one-day reminders with event details and organizer contact.
- Billing Profile Dashboard: Organizer and admin profile pages now show simple read-only billing summaries from persisted payment records, with admin organizer rollups and scheduler health at the top.
- Automatic Refund Handling: Paid webhook registrations now auto-refund the full attendee charge if final registration fails, while keeping refund failures visible for support follow-up.
- Strict Startup Config: Backend startup now fails when critical database, auth, mail, Stripe, client URL, or CORS env vars are missing instead of silently using insecure fallbacks.
- Organizer Timer Access: Event organizers now always see the in-progress timer and transport controls for events they manage, even if they are not also registered attendees.
- Demo Script Removal: Outdated helper scripts that created demo or admin accounts with hardcoded passwords were removed from the backend.
- Password Rule Alignment: Password reset now uses the same six-character minimum as signup so auth validation stays consistent across the app.
- Minimal Health Endpoint: The backend now exposes a small health endpoint with database connectivity and latest auto-complete scheduler status, and the root README shows a curl example.
- Limiter Config Simplification: The backend no longer requires a separate limiter database URL and now uses the simpler default rate-limit storage setup.
- Reminder Feature Removal: The nightly attendee reminder-email job and its tracking migration were removed, while registration-confirmation emails remain in place.
- Backend Entry Cleanup: Redundant backend runner and maintenance script files were removed so `start.py` stays the local entry point and `wsgi.py` stays the deployment import target.
- Stateless Reminder Emails: The nightly 9:00 PM Eastern attendee reminder emails are back, but now run without reminder-tracking columns or a dedicated tracking migration.
- Alembic Stack Removal: The backend no longer depends on Alembic or Flask-Migrate, and schema changes now rely solely on the checked-in SQL scripts.
- Sentry Error Monitoring: The API can now send production exceptions to Sentry through a minimal startup integration that activates when `SENTRY_DSN` is configured.
- Scheduler Worker Setup: The repo now includes a dedicated scheduler entrypoint plus a checked-in `scheduler/` folder with a minimal `systemd` unit and rollout steps for EC2 deployment.
- Email Queue Delivery: Password reset, registration, reminder, and waitlist emails now flow through a persisted `email_jobs` queue that the scheduler worker delivers instead of spawning web-worker threads.
