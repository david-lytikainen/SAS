i like to be HEAVILY involved in all design discussions
do NOT edit my wording in AGENT_TODOS or QUESTIONS

for each each directory that starts with a number (e.g. 0_KC):
	Read CONTEXT
	Read 0_QUESTIONS
	if you have a follow up question, add under the original question indented with "- Follow Up:" at the beginning and then add a "\n" and enough "\t"s to be indented below the follow up.
	if a question in {dir}/0_QUESTIONS has been answered clearly enough to implement (or discard if I dont want it), you may move that item to ANSWERED and create a concrete actionable item in {dir}/1_AGENT_TODOS with a three word title and brief subtitle. Do this only when the implementation scope is clear and does not require guessing
	if you have any questions about this project, either current state or future state, add them to 0_QUESTIONS

	write code to complete any TODOs in 1_AGENT_TODOS (or answer any questions I put in there)
	when doing a sql change, give me a sql file to run the changes in {{project}-api}/sql

	If unclear, ask a question in 0_QUESTIONS so i can answer and you can later complete it
	when you add a question to {dir}/0_QUESTIONS, add it to the top of the file under UNANSWERED and please add a "\n\t\n" after it so it is easy for me to respond. make questions bulleted with "-"
	Err on the side of asking too many questions before coding
	if you complete any AGENT_TODO that is a User-facing feature, add it to the bottom of FEATURES. give each feature you add a three word title then a colon with the rest of your brief description
	If you have any TODOs for me (stripe secrets,etc), add them to 2_TODOS
	If you feel you have completed one of your 1_AGENT_TODOS, leave a very brief indented comment below it stating your change, then move it below all the other non completed TODOs to the top of the DONE lisT. dont add newlines in between done agent_todos

	finish by reading through 0_QUESTIONS and move any questions with a new follow up to the top of the file. Move any questions that I have clearly and fully answered to ANSWERED
	
when done with your changes, git add ., git commit, and git push for all repos that you touched 
