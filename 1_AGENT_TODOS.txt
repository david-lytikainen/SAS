TODO
- dont code yet: question: can we use the user's browser to let us know where they are so we can add a filter to show all events within 2 hours or something?
- sqlalchemy.exc.ProgrammingError: (psycopg2.errors.UndefinedColumn) column events_attendees.registration_confirmation_sent_at does not exist LINE 2: ...ation_date AS events_attendees_registration_date, events_att... why am i getting this error? did you add this column? what is it used for practically? why did you not give me a sql script for it

DONE
- wait no add back the attendee reminder emails, i meant to just remove the table which monitors them and stuff.
	Restored the 9:00 PM Eastern reminder emails without bringing back the reminder-tracking table or migration.
- attendee reminder emails. im not going to create taht table. i dont want those emails to be monitored. dont care about it. remove that stuff. unnecessary
	Removed the nightly attendee reminder job, deleted its unused tracking migration, and kept only registration-confirmation emails.
- i dont have a limiter DB. just remove whatever logic you put in for that please.
	Removed the separate limiter database requirement and simplified rate limiting back to the built-in storage setup.
- where is the scheduler invoked?
	Confirmed and documented through cleanup that the embedded scheduler is started from `start.py` for local runs and `wsgi.py` for deployment imports.
- what is this for? scripts/auto_complete_due_events.py
	Removed the old maintenance script because the embedded scheduler now owns that auto-complete job.
- can you delete all scripts in sql that are no longer used in sas-api?
	Deleted the obsolete attendee-reminder tracking migration and the old event-timezone add script that no longer matches the backend.
- why do i have a run.py and a start.py? i know i usually use start.py. is run.py needed? same with wsgi.py??
	Removed the redundant `run.py`; `start.py` remains your local runner and `wsgi.py` remains the deployment entry point.
- fail startup when critical production env vars are missing instead of silently falling back to insecure defaults
	Added strict required-env checks for database, limiter, auth, mail, Stripe, client URL, and CORS config so production startup now fails closed.
- remove outdated local helper scripts that create demo/admin accounts with simple hardcoded passwords
	Removed the outdated account bootstrap scripts and stopped the remaining seed flow from calling the deleted admin helper.
- make organizer timer controls show the same as admin timer controls for in-progress events they manage
	Changed the timer visibility gate so event organizers always see the live timer and control panel for events they can manage.
- keep password minimum aligned at six characters everywhere instead of requiring eight during reset
	Reset the password-reset form minimum back to six characters to match signup and the rest of the app.
- add a very simple MVP Billing section above Profile on the profile page, with separate organizer and admin views and only the most important payment/support numbers
	Added a read-only profile billing dashboard backed by persisted payment records, with organizer summaries, admin organizer rollups, and top-level admin scheduler health cards.
- add automatic Stripe refund handling when paid checkout succeeds but final registration fails, and surface any refund failures clearly for follow-up
	Added persisted payment tracking plus automatic full-charge refunds on registration failure, with failed refund states surfaced in the billing dashboard for follow-up.
- send attendee registration-confirmation emails plus 1 month / 1 week / 1 day reminder emails at 9:00 PM Eastern with core event details, organizer contact, and a direct site link
	Added backend registration confirmation emails, nightly Eastern reminder scheduling, and registration-level send tracking so attendees only receive each reminder once.
- in fast API I really like the way you can add a scheduler to the lifespan. Is there a way to do this with flask? I am asking because I want you to set up the Cron for auto ending events which we talked about earlier I don't want to have to do where I have to manually set up a cron in the server. Is there a way to do what I am asking?
	Added an embedded Flask scheduler that runs the 9:00 AM Eastern event auto-complete job inside the app and uses a database advisory lock so duplicate web workers do not all execute it.
- I really like the look of the timer little box thing with the gear on the far right I think but the little thing underneath it that has like the start round resume round stuff which seems to be like floating when you hover over it I don't really like. Below the timer box thing for the admin and event organizer view, add a backward play/pause and forward button to be almost like a Spotify thing for a song. The backward button is for the same way Spotify does either going to the beginning of this round or going to the previous round if we are only within the first couple seconds, I guess. The play or pause button is the resume or pause round. The forward button is skipping to the break of this current round if any of this is ambiguous ask me a question.
	Replaced the floating admin timer buttons with transport-style previous, play/pause, and forward controls while keeping start, pause, resume, restart, previous-round, and skip-to-break coverage.
- make it so that the user's token can stay on their client browser for a really long time because I don't want them to continually have to sign in every time especially with a site like this that they'll only probably use like once or twice. Is there a way to make it so that they're token doesn't expire for a year because they'll sign up for the event which will be in probably a month or two for when they go and then when they open up the thing again, it will already have their account and all those and all that is this super insecure and if this doesn't work, how do I make it work? I really like how Instagram auto logs you in.
	Extended JWT access-token lifetime to one year by default so the existing persistent-login flow can keep users signed in across infrequent visits, while noting that this is a convenience-first tradeoff compared with a refresh-token design.
- abstracting event management: since the goal of this is to empower event organizers to plan speed dating events is there anything else that I need in my app to allow this to happen? Or am I ready to hand this off to a user who can become an event organizer and let them go. Does the stuff like platform fee and all those things already work out? Where the event organizer will get a chunk of the price per person I will get a percentage of it plus a flat rate and then obviously stripe will get some of it too. Please answer this question or ask me a follow up.
	The core Stripe split logic is wired for paid attendee signup and connected-organizer payouts, but I would not call it fully handoff-ready yet without production env setup for Stripe webhooks/mail/cron plus a pass on organizer reporting, refund ops, and production monitoring.
- I think I like the bubble you created, but I want it to be right below the NAV bar and pointing to the middle of the sign in button right now it's pointing up to the S. Also, right now it looks like it has width 100. I want it to have around width 50 or 30. So yeah right now it's pointing up to the word faith when I actually want it to be above the Saved and Single header and just below the NAV bar
	Moved the signed-out landing bubble directly under the nav, centered its arrow, and narrowed the callout so it points toward sign-in instead of the page header.
- go through all of FEATURES.txt and give all the features that don't have a three word title a title
	Retitled every existing feature entry into the requested three-word-title format while keeping the same chronological order.
- UX landing page: on the landing page create a little bubble that has an arrow up to the sign in button if a user is not signed in make the Bible say "to attend or organize events". I want this to show the user that once they sign in or sign up they will be able to see the events page and from there they will be able to either search for an event or create one.
	Added a signed-out landing-page callout bubble that points toward sign-in and explains users can attend or organize events after logging in.
- on the event create card, add a boolean checkbox, which will determine whether or not the 60/40 weight lifting rule will be enforced for this event so that the event organizer can have the choice to choose whether they want that for their event. have a very brief description under it describing what it means if the user Jun checks the box. Make it true by default. If you need a sequel change, put it in the sql dir as a script
	Added an event-level 60/40 gender-balance toggle in create/edit flows, defaulted it on, backed it with a SQL migration, and wired the backend registration logic to respect it.
- long chat history: im sure you have picked up on what i am asking you to do... will you get worse over time since i use resume every time? should we only retain the last 30 codex calls or something?. dont code just discuss
	Resume itself is fine; the real risk is stale context, not raw age. A last-30-days resume habit is reasonable if durable decisions stay in repo files and old resolved chatter is allowed to drop.
- round time controls: when can an EO/Admin edit the round and break duration? any concerns there?
	Confirmed the current behavior stays in place: timer settings are editable for managers before a round starts and during break time, but not while a round is actively running or paused.
- review the current 60/40 gender ratio waitlist rule in the code. is there anything we need to update? does it work with current functionality?
	Updated the waitlist flow so the 60/40 rule still gates real registration, while waitlisted users are no longer promoted directly and instead get emailed to come sign up themselves when space opens.
- flowchart: can you instead put the flowcharts svgs in the readme so they can display when i look on github mobile app? is that possible? if so, make it clear in cron2 what to do instead of pushing the svgs to the repo itself
	Added a root README that displays the rendered flowchart SVGs and updated `cron2` so the flowchart cron keeps those README images current for GitHub/mobile viewing.
- when an attendee registers for an event is the checkout screen already configured to show up and not let them sign up unless they pay? If not, please add this. If it is added, what stripe account does it pay? The one that I have set up in my .env?
	Confirmed the checkout UI was already wired, then enforced paid signup through Stripe Checkout in the backend too; organizer-created paid events transfer payout to the organizer's connected Stripe account, while admin-created paid events charge your platform Stripe account directly.
- no git pushes: in the last 8ish runs you have done, seems like you did not push your changes because they got picked up in my "logs" commit - why is this?
	Updated the cron runner so it now commits and pushes `sas-api`, `sas-ui`, and the root repo separately instead of only pushing the root logs repo.
- THIS IS A TODO, put a question in 0_QUESTIONS IF YOU NEED MORE INFO FOR THIS. update the SAS/cron1 prompt so that it not only updates the flowchart.MD, but after it is done, it actually creates the image of the flow chart and then publishes that to the current branch Also, I guess just to get or whatever. 
	Updated `cron1` and added a Mermaid SVG render script so the cron flow now publishes committed flowchart images.
- next morning cron end event: remove all the weird coding you did, not a huge fan. instead, replace it with just 1 cron at 9am the next morning EST. i like this compromise because even accross the country, I am ok with a 6am cron. please give me sql to revert the original stuff you gave me for adding the timezone
	Replaced the request-time timezone logic with a single 9:00 AM Eastern cron flow and added SQL to drop the event timezone column.
- Better title for tab (<title> i think?)
	Reverted the tab title back to just Saved & Single.
- question: how do you automatically end the event? Did you use a cron job or something like that? That is definitely what I would prefer.
	Added a cron-friendly backend script for due-event auto-completion; scheduler hookup is listed in 2_TODOS.
- profile page: when the pencil is clicked, dont show the pencil anymore and just show a check icon and the X icon where the check will save any changes and it is only enabled if any changes have been done. the X will keep the same functionality. this replaces the "Save Profile" button at bottom
	Replaced the header pencil with save/cancel icons in edit mode and disabled save until changes exist.
- Event ends automatically the next morning instead of being Ended by clicking the admin button 
	Added persisted event timezones, automatic next-morning completion, and removed the manual End button from the UI.
- Make sure UTC is fine for all dates and date times
	Persisted event timezones, tightened UTC/local event datetime handling, and fixed date-only birthday timezone drift.
- when the profile page is read only don't make it obvious that it is an input field make it look a little bit better and you don't need the 18+ only label under the birthday and remove the gender selector, and when they are in adult mode show and X icon next to the profile that they can click to stop editing also make the pencil icon and the X icon, which is next to the profile header have like a rectangular but rounded outline. Also remove the logout button from the NAV bar and simply add it to the bottom of the profile card on the profile page left justified and just make it look kind of nice also remove the subheading under profile
	Polished the read-only profile view, added outlined edit/cancel controls, and moved logout onto the profile card.
- make it so that the profile page is accessed by clicking on the username in the top right corner instead of having a separate tab called profile. Also, by default show all the values for the profile that you already have shown in a read only format, but then if they click the pencil icon next to the header that says profile, then it will make those fields editable and the safe profile on the bottom right will appear.
	Updated profile access to the top-right username and made the form read-only until the header pencil icon is clicked.
- Same 2 people wont date more than once (across events) boolean for EOs? for every event automatically?
	Filtered prior speed-date pairs out of future schedule generation.
- profile page to update user things
	Added an authenticated profile page and backend profile update route for core account fields.
- Check in without pin (after stripe): remove the pin code for attendees. instead, EOs can just type in a name and check them in manually. instead of the view pin thing i guess. removd the check in for attendees
	Implemented manual organizer/admin attendee check-in in registered users, and removed attendee PIN check-in from the web UI.
- Add formatting to new church text (no nasty text)
	Normalized and sanitized custom church names in signup and attendee/waitlist editing flows.
