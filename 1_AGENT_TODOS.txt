TODO
- update the SAS/cron1 prompt so that it not only updates the flowchart.MD, but after it is done, it actually creates the image of the flow chart and then publishes that to the current branch Also, I guess just to get or whatever.
- question: how do you automatically end the event? Did you use a cron job or something like that? That is definitely what I would prefer.
- profile page: when the pencil is clicked, dont show the pencil anymore and just show a check icon and the X icon where the check will save any changes and it is only enabled if any changes have been done. the X will keep the same functionality. this replaces the "Save Profile" button at bottom

DONE
- Event ends automatically the next morning instead of being Ended by clicking the admin button 
	Added persisted event timezones, automatic next-morning completion, and removed the manual End button from the UI.
- Make sure UTC is fine for all dates and date times
	Persisted event timezones, tightened UTC/local event datetime handling, and fixed date-only birthday timezone drift.
- when the profile page is read only don't make it obvious that it is an input field make it look a little bit better and you don't need the 18+ only label under the birthday and remove the gender selector, and when they are in adult mode show and X icon next to the profile that they can click to stop editing also make the pencil icon and the X icon, which is next to the profile header have like a rectangular but rounded outline. Also remove the logout button from the NAV bar and simply add it to the bottom of the profile card on the profile page left justified and just make it look kind of nice also remove the subheading under profile
	Polished the read-only profile view, added outlined edit/cancel controls, and moved logout onto the profile card.
- make it so that the profile page is accessed by clicking on the username in the top right corner instead of having a separate tab called profile. Also, by default show all the values for the profile that you already have shown in a read only format, but then if they click the pencil icon next to the header that says profile, then it will make those fields editable and the safe profile on the bottom right will appear.
	Updated profile access to the top-right username and made the form read-only until the header pencil icon is clicked.
- Same 2 people wont date more than once (across events) boolean for EOs? for every event automatically?
	Filtered prior speed-date pairs out of future schedule generation.
- profile page to update user things
	Added an authenticated profile page and backend profile update route for core account fields.
- Check in without pin (after stripe): remove the pin code for attendees. instead, EOs can just type in a name and check them in manually. instead of the view pin thing i guess. removd the check in for attendees
	Implemented manual organizer/admin attendee check-in in registered users, and removed attendee PIN check-in from the web UI.
- Add formatting to new church text (no nasty text)
	Normalized and sanitized custom church names in signup and attendee/waitlist editing flows.
- Better title for tab (<title> i think?)
	Updated the browser tab title to a more descriptive Saved & Single title.
