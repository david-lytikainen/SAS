TODO
- above the forgot password sign on the sign in or the login thing. Add a login with one time email code using their email as an alternate way of logging in
DONE
- is wsgi setup all correctly? is it a copy of start?
	Yes. Both create the same Flask app, but `start.py` runs the local debug server while Gunicorn correctly imports `wsgi:application` without starting it itself.
- make the light mode, dark mode, toggle on the profile page and make the default light mode
	Moved the sun/moon control to the Profile header and changed the shared theme default to light mode.
- right now most of the website is black background. Can you add a very simple light mode switch in the NAV bar that has white instead of black in those places just so I can gauge which one looks better
	Added a navbar sun/moon toggle that switches the shared app theme between the current dark mode and a light preview.
- Require Gender Checkins: Prevent schedule generation until checked-in attendees include at least one male and one female.
	Blocked the organizer flow and direct API requests until both checked-in groups are present.
- is there anything that is not built well/not production ready?
	The core event, payment, and dedicated scheduler paths are in reasonable shape, but I would not call the deployment production-ready until the remaining operational gaps are decided: there is no automated database backup and restore plan; the current rate limit is process-local, so the two Gunicorn workers do not share it; and Sentry is not present in the current code or dependencies despite an older note saying it was implemented. Immediate email failures intentionally only log, there is no automated smoke-test/CI path, and the home-server static-IP question is still open. The backup, shared rate-limit, and Sentry decisions are now in `0_QUESTIONS`.
- what about this CORS_ORIGINS=http://localhost:3000
	This is the browser-origin allowlist for credentialed API requests. Use `http://localhost:3000` when the local React frontend calls the local API. On the home server use `https://SAS_DOMAIN.COM` with no trailing slash; if a local frontend must call the hosted API during development, use both values separated by a comma.
- waht is this used for? CLIENT_URL=http://localhost:3000
	This is the public SAS frontend address. The backend uses it to build password-reset, registration confirmation, waitlist, and reminder email links; it also gives Stripe Connect the organizer website URL outside local development. Locally use `http://localhost:3000`; on the home server use `https://SAS_DOMAIN.COM`.
- CREATE TYPE registrationstatus AS ENUM ('REGISTERED', 'CHECKED_IN', 'CANCELLED', 'WAITLISTED'); why are we using any enums? i thought we had no enums and all were just strings? i think I like taht better 
	You are right: the current SAS code still uses PostgreSQL enums for gender and registration status, while event status is already a string. The startup script reflected the current models; a strings-only conversion needs an explicit data migration and model cleanup, so the implementation decision is now in `0_QUESTIONS`.
- what will these be? how will I know the port and i think i know the domain name? STRIPE_CONNECT_REFRESH_URL=http://localhost:3000/events?view=create&stripe_connect=refresh STRIPE_CONNECT_RETURN_URL=http://localhost:3000/events?view=create&stripe_connect=return STRIPE_CHECKOUT_SUCCESS_URL=http://localhost:3000/events?view=create&checkout=success&session_id={CHECKOUT_SESSION_ID} STRIPE_CHECKOUT_CANCEL_URL=http://localhost:3000/events?view=create&checkout=cancelled
	Use your public `https://SAS_DOMAIN.COM` URLs exactly as documented in `2_DAVID/hosting.md`. These are browser return URLs, so they never include Gunicorn's internal port `8001`; Nginx accepts public HTTPS on port 443 and forwards API traffic internally. Your configured Checkout success URL keeps `view=create&` because the backend removes that part before returning the attendee to the normal events view.
- is this needed STRIPE_CONNECT_COUNTRY=US
	No. The backend already defaults this to `US`, so it is optional. Keeping it in the environment file is only useful as an explicit reminder that SAS supports US Stripe Connect accounts only.
- i want to test with my hosting thiing on my home server how do i set this thing up? STRIPE_WEBHOOK_SECRET=whsec_019d4f004ef797569af7dd27096b7e789052f5cd6aa2bd8cc97fa98dafe25885 before i was using stripe listen --forward-to localhost:5001/api/user/stripe/webhook
	After your domain points to the home server and HTTPS works, create a test-mode Stripe Dashboard webhook endpoint at `https://SAS_DOMAIN.COM/api/user/stripe/webhook` for `checkout.session.completed` and `account.updated`. Put that endpoint's signing secret in `/srv/sas/.env`, restart `sas`, then use Stripe's Send test event. The CLI secret only verifies events forwarded by `stripe listen`; it must not be reused for the Dashboard endpoint. Rotate the secret pasted here before using it.
- is this needed STRIPE_PLATFORM_FEE_PERCENT=10? 
	No. SAS does not read this variable. Fees are currently defined in code as $1 plus 5% for an organizer's first two events, then $1.50 plus 8%; remove this unused variable from the environment file.
- after they click generate schedules, default the number of tables to min(num_males, num_females)
	The schedule dialog now defaults tables to the smaller checked-in male or female count, while keeping one as the lowest valid value.
- is there anyway with G unicorn or uvicorn to decrease the amount of RAM used for a process for one that is specified as being less ram intensive?
	Yes: run that service with one worker and the basic sync worker class (`gunicorn --workers 1`) rather than extra workers or threads. `--preload` can share read-only startup memory across multiple workers, but it is not useful for a one-worker service. Gunicorn/Uvicorn cannot safely make the app itself need less RAM beyond that; `systemd MemoryMax=` only caps it and can kill it if exceeded. For SAS, the scheduler already runs as one dedicated process, which is the right lower-RAM shape.
- Operational Event Logging: Add concise server logs for startup, auth, registration, checkout, refunds, scheduler, and email failures.
	Added key lifecycle and failure logs while keeping secrets, tokens, email addresses, and payment details out of operational logs.
- Scheduler Startup Design: Explain the memory tradeoff of the current reusable app startup.
	It is a separate process and does not serve web requests. It loads the same app only to reuse database, email, Stripe, and event setup; that adds some idle RAM but avoids duplicated configuration.
- Reminder Queue Throughput: Poll the reminder queue every minute and send up to 100 emails per pass.
	Reduced polling to one minute, increased batches to 100, and removed the parent repository workflow file.
- why can't the polling be more than every five minutes and why does it send it in batching of 25? Can't we make both of these better?
	Both were cautious defaults, not limits. For SAS's expected size, I recommend polling every minute and sending up to 100 reminders per pass; only a mail-provider limit would justify lower numbers.
- Reminder Queue Interval: Poll the queued reminder emails every five minutes instead of every fifteen minutes.
	Reduced the reminder queue polling interval to five minutes while keeping batches capped at 25.
- with our goal being multiple 100% events per month. Is it worth keeping it? Should we increase the time to less than 15 minutes?
	Keep the reminder queue for controlled, visible delivery bursts. I recommend polling every 5 minutes: batches of 25 can deliver 100 reminders in about 20 minutes instead of roughly an hour.
- do you mean some of the emails are now in the non-scheduled process? And what is the queue for anyways? Is there any benefit for it?
	Yes. Password resets, registration confirmations, and waitlist notices now send from the normal API process; only reminders are queued. The queue records reminder delivery, caps batches at 25, and preserves sent/failed history, but adds little value at small scale.
- Immediate Account Emails: Send password reset, registration confirmation, and waitlist spot-open emails from the normal app process; keep reminders queued for the scheduler.
	Moved the time-sensitive email types to direct delivery and left reminder emails queued.
- Stripe Return Session: Preserve Stripe's checkout-session placeholder in the return URL and reject invalid return IDs before Stripe is called.
	Fixed percent-encoding of the placeholder and added browser/API guards for invalid session IDs.
- OPTIONS /api/user/signin HTTP/1.1" 200 - ERROR:app:Login error: Missing required argument 'digestmod'.
	Verified the pinned JWT stack creates tokens correctly; added the virtual-environment rebuild command to `2_TODOS` for the conflicting deployed package.
- does SAS already have the 365 day JWT?
	Yes. SAS defaults access tokens to 365 days, with `JWT_ACCESS_TOKEN_EXPIRES_DAYS` able to override it.
- Stripe Industry Prefill: Set merchant category `7999` for new organizer Stripe Connect accounts.
	New organizer accounts now send Stripe the approved speed-dating category.
- Legacy Task Review: Reviewed the older task list and moved its unresolved Stripe category decision into this unified list.
	The remaining unapproved choice is recorded in `0_QUESTIONS`.
- Email Retry Removal: Simplified the dedicated scheduler worker and removed automatic failed-email retries.
	Failed sends now remain failed for support review; they are not requeued.
- Documentation Filename Consolidation: Merged the duplicate task list and renamed workflow files without `.txt` suffixes.
	The SAS workflow now uses one current file for each instruction document.
- Comparable Build Estimate: Answered the pricing question for a comparable custom build.
	A comparable custom build would typically be priced around $35,000-$90,000, depending on polish, support, and deployment scope.
- Scheduler Worker Cleanup: review the dedicated scheduler code and setup guide, then remove unnecessary complexity while keeping a separate scheduler service.
	Only the dedicated scheduler runner now starts scheduled jobs; the web app does not.
