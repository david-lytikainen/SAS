- Enter your sudo password and run `sudo systemctl restart sas` after the current API pull; this shell cannot provide the password, so the live service is still on the pre-pull code.
- Rebuild the SAS server virtual environment so sign-in loads the pinned PyJWT package instead of a conflicting `jwt` package: `cd /srv/sas/api && rm -rf /srv/sas/venv && python3 -m venv /srv/sas/venv && /srv/sas/venv/bin/pip install --upgrade pip && /srv/sas/venv/bin/pip install -r requirements.txt && sudo systemctl restart sas sas-scheduler`.
- Set strong production env vars for `JWT_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `MAIL_SERVER`, `MAIL_USERNAME`, `MAIL_PASSWORD`, `CLIENT_URL`, and `CORS_ORIGINS`.
- Set production `DATABASE_URL`, because backend startup now requires it instead of falling back to a local default.
- Run `sas-api/sql/001_startup.sql` before enabling the dedicated scheduler worker, because queued email delivery now depends on the consolidated current schema.
- If your deployed frontend paths differ from the current `/events?...` defaults, set explicit `STRIPE_CONNECT_REFRESH_URL`, `STRIPE_CONNECT_RETURN_URL`, `STRIPE_CHECKOUT_SUCCESS_URL`, and `STRIPE_CHECKOUT_CANCEL_URL`.
- Set the frontend API base for each deployed environment through `REACT_APP_API_URL` or an equivalent runtime `window.REACT_APP_API_URL` value now that localhost is no longer hardcoded.
- Decide where production logs, webhook failures, and outbound-email failures will be monitored, because the app currently logs these server-side but does not yet have an in-app ops dashboard.
- Install the checked-in `scheduler/sas-api-scheduler.service` unit for the separate scheduler process.
- Confirm which privacy/security claims your real hosting stack can support before tightening the privacy-policy wording around TLS, encryption at rest, and phone-verification/SMS behavior.
- Rotate the Stripe webhook signing secret that was pasted into `1_AGENT_TODOS`, then save the new Dashboard endpoint secret as `STRIPE_WEBHOOK_SECRET` in `/srv/sas/.env`.
- Create a free Sentry project, then save its DSN as `SENTRY_DSN` in `/srv/sas/.env` and restart `sas` and `sas-scheduler`.
- Run `sudo systemctl daemon-reload && sudo systemctl restart sas.service` after entering your sudo password so the active service picks up its changed unit definition.
