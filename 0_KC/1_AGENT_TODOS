TODO
- Sold Gallery Pricing: when a priced gallery item is purchased, clear its price so it becomes a view-only gallery item.
- why so many fliwcharts in this dir? rm the depreciated ones
DONE
- Per Image Ordering: add a simple per-image reordering control in Edit Gallery so Kyra can choose the image order inside one gallery item.
	Added simple up/down image ordering in Edit Gallery and made the first saved image become the cover everywhere else in the app.
- flowchart words still overflowing past the bottom of rectangles
	Shortened and split the flowchart nodes, then re-rendered the SVGs so the text stays inside the boxes more reliably.
- Gallery Image Crop: add the simplest local crop step before save-time gallery image upload.
	Kyra can now crop each selected gallery image locally with simple zoom and position controls before save-time upload.
- the flow chart words in the rectangles are overflowing below the rectangle a little bit, and it's hard to read. Can you make the rectangles with the words in it the right size so that the words are inside of them
	Shortened and wrapped the flowchart labels, then regenerated the SVGs so the text stays inside the boxes more cleanly.
- flowcharts look good but now stack them vertically one on top of the other so i can view each one at a time on my phone
	KC now renders separate admin, commission, and gallery flowchart SVGs that stack vertically in the README.
- make the cursor on the main image zoom to where the cursor is like they do on the front image for amazon, also remove the ask a question thing on the gallery page for items that dont have a price
	The gallery detail image now zooms toward the cursor, and unpriced items no longer show the question panel.
- Local Flowchart Rendering: replace the remote Mermaid render dependency with a local renderer so KC SVG refreshes do not depend on remote services.
	KC flowcharts now render locally through a cached Node Mermaid runtime with no remote render dependency.
- squash all sql files into one startup file. 
	KC now has a single `sql/001_startup.sql` that includes the later backfills too.
- make it so that the admin can add up to 5 images for each gallery item. 
	Admin gallery items now save up to 5 ordered images under one shared item.
- make a gallery item page per gallery item so that when clicking on a gallery item it blows it up and any other pics for that item. keep buy and ask a question buttons on the gallery page
	Added `/gallery/{item_id}` with a large image view, previews, buy, and inquiry actions.
- why a separate table for gallery inquiries and gallery orders? does it make the most sense? when they buy it on an inquiry, does it then create a gallery order?
	Yes; separate tables fit the current code better, and buying from an inquiry currently creates a new paid `gallery_order` while the inquiry stays as its own record.
- when posting a comment right now on the UI do you wait until the email sends to show the message? there seemed to be some latency between post and seeing the comment. the correct flow: types comment -> clicks button to post -> comment shows up on UI as posted immediately -> saving done in backend -> if error, show error on comment small message else nothing -> email is sent -> if email error show on UI else show muted "Email has been sent" below comment subtly
	Comments now render in the UI immediately, email delivery runs after save, and each new comment row updates with a subtle sent state or a small email error.
- flowcharts are still impossible to see on a vertical rectangle like an iphone. i want one box per row, three start points. they dont all have to be linked but instead a vertical sequential flow from each start point. admin sign in on top, below all admin boxes customer commission req, below that gallery inquiry/order
	Rewrote the flowchart into three stacked vertical sequences for mobile readability and attempted to refresh the KC SVG, but the remote renderer timed out.
- Email Template Folder: move the current inline plain-text email bodies out of `kc-api/app/routes.py` into a dedicated mail template folder.
	Moved the plain-text mail bodies into `kc-api/app/emails/` templates and routed mail rendering through that folder.
- I don't think we need the onReady stuff on the homepage anymore since the gallery is only ever one row. Right?
	Removed the homepage gallery-ready handshake and scroll gate, since the current one-row gallery no longer needs it.
- Inquiry Contact Capture: require an email and message when asking a gallery question so Kyra can see who asked before checkout.
	Added required gallery-inquiry email capture in the question prompt and now save that email on the inquiry before checkout.
- revert cron.sh change
	Reverted `cron.sh` back to the simpler unconditional `git add . && git commit && git push` ending.
- add simple UI masking to all email and phone inputs to ensure the correct format
	Added simple email normalization and phone masking to the live admin sign-in and commission request inputs.
- where are the email HTML files right now? I would want them all in a folder. Is that how they are now?
	There are no HTML email files yet; the app currently sends inline plain-text mail bodies directly from `kc-api/app/routes.py`.
- Numbered Flowchart Refresh: make numbered-project flowchart rendering/README updates work from `0_KC` source and refresh the flowchart to use the requested 3 entry points.
	Updated the root renderer to pick up numbered-project flowcharts and refreshed `0_KC/flowchart.md` plus the rendered SVG to the requested 3-entry-point structure.
- Inquiry Order Rows: show gallery inquiries in Kyra's `All Orders` list before they become paid gallery orders.
	Added explicit gallery inquiry labeling in Kyra's `All Orders` cards, using the existing admin feed rows.
- Confirmation Checkmarks: show a small green checkmark on customer-confirmed delivered orders in Kyra's `All Orders` cards and on the shared order page.
	Added green confirmed checkmarks to admin order cards and the shared order page when receipt confirmation exists.
- does the orders table keep track of customer email right now?
	Yes; `commission_requests`, `gallery_orders`, and `gallery_inquiries` all store `customer_email`, and gallery orders are filled from Stripe checkout customer details when payment completes.
- how is stripe configured to create a price ID right now? Or is that not configured yet?
	There is no saved Stripe Price ID path yet; checkout uses inline `price_data` in `kc-api/app/routes.py` for commission, gallery, and gallery inquiry purchases.
- update the bottom of cron.sh to only git add and git commit if nothing but the logs file is changed
	Changed `cron.sh` so it only adds, commits, and pushes when `logs/cron.log` is the only repo change.
- can you paste agent bots current cron tab into a file called crontab
	Saved the current `crontab -l` output to `0_KC/crontab`.
- she wants a customer confirmed button after delivery that they can check on the order page with a yes no confirmation to confirm that they received it.
	Added a delivered-order receipt confirmation flow on the shared order page, with a yes/no confirmation step and saved confirmation timestamp.
- email to be sent out each time the admin changes the status
	Admin status changes now send a simple order-status email to the customer, including quoted, declined, accepted, in progress, shipped, and delivered changes.
- Gallery Order Comments: add the same customer/admin comment thread to paid gallery order pages.
	Added paid gallery order comments with the same shared customer/admin thread, edit/delete flow, and new-comment email behavior.
- add a comment icon next to the Buy button on the gallery item (when it has a price). make it so that a person can "Ask a question" about a gallery item which will create a gallery "order" type page with random 6 digits. this will be called a gallery inquiry. when they click the comment icon, a small text box appears and they can type and then send the message. when sent, takes them to the newly created gallery inquiry page with its own comment section , a loaded pic of the gallery item and a Buy button for the user which takes them to stripe checkout.
	Added gallery inquiries with a question composer, shared order page, inquiry comments, and inquiry-specific checkout into a paid gallery order.
- change all occurences of Category/Categories to Service/Services
	Updated user-facing commission copy from category/categories to service/services in the active UI and API responses.
- when she drags a gallery item make it auto save the new order 
	Dragging a gallery item now saves the new order immediately on drop and shows saving/saved feedback inline.
- make name, email, phone, category, instructions, medium, and size required fields for requesting a commission
	The commission form and backend now enforce required name, email, phone, service, instructions, medium, and size fields.
- make delete button on Edit Gallery have a confirmation button
	Added a two-step delete confirmation in Edit Gallery, with confirm and cancel actions on the selected card.
- Gallery Order Page: make gallery purchases open into a dedicated order page that is very similar to the commission request page, using the existing gallery order records plus shipping details.
	Added a gallery order page on the shared order route, with payment-processing handling, shipping details, customer order-link email, and admin shipment status controls.
- add an optional price field to the gallery items. If a gallery item has a price, show a small buy button in the bottom right corner of the gallery item when they're reviewing it on the homepage, this will also use stripe. If a gallery item does not have a price don't show it on the gallery page.
	Added optional gallery pricing, Stripe checkout, webhook-backed gallery orders, and mixed admin order rows for paid gallery purchases.
- remove the email button from the comments thing. instead, the email will be sent automatically for each new comment (not for editted ones)
	New comments now auto-email the other side with the comment anchor link, and edited comments no longer resend mail.
- when adding a gallery item why does it upload the image to s3 before she creates the item, but in the request commission it only adds to s3 when they fully request it? this is probably the better way for both, no?
	Moved gallery image upload to save-time, kept instant local preview, and removed abandoned draft uploads to S3.
- i wasnt able to run some of the statements in the startup scripts... look over them again. clean them up, make them simpler and make them all in one file
	Removed the extra lookup-table SQL file and kept the startup schema in one simpler startup script.
- on her profile, i want admin tools to be a heading like profile then for each different tool, I want it to be an accordion the way you have it is nice for now (style wise). so basically one accordion for creating a gallery item, one accordion called Edit Gallery which shows only the titles of each picture under each title is the edit or delete buttons. on the top right of each rectangle is a little draggable icon thing that shows it can be dragged. for mobile show 2 gallery items per row, ipad show 3, pc show 5. one accordion called Categories for her to add edit and do stuff with categories. then all orders can stay the same
	Restructured the admin profile into separate accordions and made Edit Gallery responsive with title cards and drag handles.
- hardcode jwt expiration to 365 days dont make it a .env variable
	Hardcoded the JWT expiry to 365 days in the login token path and removed the env-driven setting from backend config and docs.
- Submission Email Slice: email the customer their order link and order number right after a successful commission submission using the existing SMTP setup.
	Added a submission email after order creation so customers get their order number and direct order link when mail is configured.
- only one s3 bucket for both things, will just use paths in the bucket for separation. in the readme of the backend, make the example .env in a code snippet so it can be copied with all the keys but just dummy values -> S3_BUCKET
	Switched the backend to one `S3_BUCKET` with `gallery/` and `commissions/` paths, and put a copyable `.env` example block in the README.
- for the mail, use this: MAIL_SERVER=smtp.gmail.com MAIL_PORT=587 MAIL_USERNAME= MAIL_PASSWORD=
	Replaced the old SMTP env names with the requested `MAIL_*` names and wired mail sending to them.
- Minimal Webhook Slice: add the smallest Stripe webhook path so successful checkout can mark quoted orders accepted even if the browser never returns.
	Added a verified `POST /stripe/webhook` path and reused the paid-order update path so Stripe can confirm successful checkout server-side.
- dont use enums anywhere in the backend, instead create a table in the backend with the values
	Replaced backend enum columns with seeded lookup tables for roles and commission statuses, plus a migration SQL script.
- in routes.py remove all helpers unless it is used three or more times
	Removed one-off and two-off route helpers, leaving only helpers reused enough to earn their keep.
- why is there the Settings thing still there? instead add an example .env in the readme
	Removed the typed settings wrapper and switched `kc-api` to direct `.env` loading with module-level config values.
- change the admin email and password to just a config value that i can set in the .env
	Bootstrapped the admin login from `ADMIN_EMAIL` and `ADMIN_PASSWORD`.
- why does the 3rd sql script have a rename legacy commission thing? make the sql files for startup and merge them into one file for now.
	Removed the legacy rename block and merged startup SQL into `sql/001_startup.sql`.
- i want the app to be able to be started from main.py with a lifespan function and if name == __main__ or whatever 
	Added lifespan startup and direct `python app/main.py` launching.
- separate all routes into a file called routes.py
	Moved the API route layer and route helpers into `app/routes.py`.
- are all those classes at the top of main.py DTOs? if so, add them to a separate file called dto.py and make them all seemlessly change between camelcase for the frontend and snake case for the backend. 
	Yes; moved them into `app/dto.py` and switched API DTOs to camelCase aliases.
- in kc-api separate the Settings thing in main.py into a .env file
	Moved runtime config out of `main.py` and added `.env.example`.
- in the readme for both kc-api and kc-ui, add a section called Setup which provides every single detail needed to start the application and run it 
	Expanded both READMEs with concrete setup and run steps.
- Manual Accept Flow: let Kyra mark an order as accepted from admin controls for off-app payments like Venmo.
	Added admin-side accepted status support and a UI button for off-app payments.
- Admin Auth Cleanup: move admin sign-in to its own direct route, remove the public sign-in button, and strip stale non-admin auth assumptions from the UI flow.
	Moved admin login to direct `/admin` routes and removed the public sign-in button.
- Anonymous Commission Slice: build the first commission request vertical slice using anonymous customer tracking, 6-digit order numbers in customer links, required submission fields, category management in Kyra admin tools, profile-page All Orders pagination, quote/decline flow, upload limits of 5 image files, and the agreed status lifecycle.
	Added real commission tables, anonymous order creation/detail pages, category management, comments, quote/decline/payment flow, and admin order pagination.
- in your last run you said i hadnt answered your questions... but I have? how did you not see that?
	This run re-read the answered questions and used them to complete the slice against your clarified decisions.
- Admin Gallery Slice: build the first private Kyra admin slice with simple auth like `sas-api`, a user profile page, an admin tools accordion on Kyra's profile, and gallery create/edit/delete/reorder support with drag-drop ordering and signed S3 asset delivery.
	Added token auth, profile pages, Kyra-only admin tools, gallery CRUD, drag-drop reorder, and signed-S3-aware gallery delivery/upload hooks.
- Gallery Metadata Slice: build the first real vertical slice for the public gallery using Postgres metadata rows as the source of truth, plus the API read path and UI rendering path.
	Added a real `gallery_items` model, `/gallery` API, SQL setup script, and UI fetch/render path for published gallery records.
- Skeleton Repo Setup: create the skeleton repos kc-api and kc-ui. the backend will be python FASTapi sqlalchemy with the following structure: app: models.py (for all tables), main.py which will be called for startup with all cors stuff and everything else, the rest can be talked about later. for kc-ui: will be React and a similar structure to sas-ui which you have worked on and can find. make as simple of a filestructure as possible for the UI, where all components basically will be in the same dir
	Created minimal `kc-api` and `kc-ui` repo skeletons with lean starter files, FastAPI/SQLAlchemy app bootstrap, and a mobile-first React shell that stays close to the existing `sas-ui` tooling.
- Root Ignore Review: does my .gitignore look ok in the root?
	Yes for the current setup. It correctly treats `0_KC/kc-api` and `0_KC/kc-ui` as separate nested repos instead of root-tracked directories.
- Workflow Clarity Review: is the entire workflow of iterating through directories and completing todos and answering questions and such clear enough with my files? or is there any obvious low hanging fruit for me to clarify in my files
	The workflow is clear enough to execute. The main low-hanging fruit was just creating missing workspace files like `FEATURES` so every numbered directory has the same durable control surface.
