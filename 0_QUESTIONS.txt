- For production auth, do you want me to keep the current one-year bearer-token approach for convenience, or should I replace it with a refresh-token flow plus server-side session revocation before launch?


- For the new timer transport controls, please confirm the exact forward/back behavior you want. My current read is: `Back` restarts the current round if there are more than a few seconds elapsed, otherwise it jumps to the previous round; `Play/Pause` toggles the live round; `Forward` ends the live round and goes into the break if there is another round. Is that correct, and should these replace the current text buttons entirely?
	yes, all your thoughts are correct. let me know if this will lose any functionality. test this thourougnly 

- For organizer/admin timer controls, when exactly do you want round duration and break duration to remain editable: only before Round 1 starts, or also while the event is paused / between rounds / already in progress? Right now the timer settings stay editable whenever the manager can access the timer controls.
	when can they currently edit? i think i like how it is already

- Until we decide the paid-event waitlist design, do you want me to temporarily disable paid-event waitlists and waitlist auto-promotion entirely so nobody can be moved into a paid event without paying?
	you can just remove the move to registered from the waitlisted people.. might make the most sense dont you think?

- For paid events, which waitlist model do you want as the next direction? My recommendation is: allow users to join the waitlist without paying, then when a spot opens send them a time-limited Stripe Checkout link and only register them after payment succeeds. If you do not want that, should paid events instead have no waitlist at all for now?
	add them to waitlist automatically no payment. if a spot opens up, email them that they can go into the site and sign up now and that spots are filling fast. dont give them any window. basically the waitlist will just be a "notify these users when there are spots" if that makes sense.

- The backend already supports updating registered attendee `phone` and `birthday`, but the current registered-users UI only exposes name, email, gender, and church edits. Do you want organizers/admins to be able to edit phone and birthday in that UI too?
	No, I took those out because it was getting too cluttered so you can take that out of the back end

- In the current UI, organizers can export waitlists and all schedules for their own events, but only admins can export the registered-attendees CSV. Should organizers also be able to export the registered-attendees list for events they manage?
	yes, event organizers should be able to export the registered attendees list for events that they created

- The backend already supports updating waitlisted user details, but the current waitlist UI only lets organizers/admins move users to registered or export the list. Do you want a visible edit flow for waitlisted users in the web UI too?
	you can remove that from the back end

- The backend timer API supports updating both `round_duration` and `break_duration`, but the current organizer/admin timer settings UI only lets you change round duration. Do you want break duration to be configurable in the web UI as well?
	Yes, add it so that the break duration is also configurable through the UI. Add it to the same pop-up panel that the break or that the round duration is editable in.

- For paid events, the organizer/admin `Move to Registered` action from the waitlist currently bypasses Stripe and registers the user directly. Is that intended, or should paid-event waitlisted users still have to complete payment before they become registered?
	See my note below about waitlisting. I'm thinking that we should rethink weight listing now that we have stripe before registration. What is the best way to do this?

- For paid events, attendees can currently cancel their registration in the UI after paying, but the app does not refund them and only shows a warning. Do you want paid-event cancellations to stay allowed with no refund through the app, or should I block attendee cancellation after payment and force organizer/admin handling?
	if they paid for their registration, they can still cancel their registration. Just make sure it is a friendly warning that they will not get refunded through the website and that they can reach out to their event organizer to get their money back.

- The waitlist flow currently tells users they will be notified if a spot opens up, but I do not see any email or in-app notification being sent when someone is moved from waitlist to registered. Do you want me to add an automatic notification for that promotion?
	before we implement any more features with the waitlist, I want to brainstorm how we should do the waitlist with the fact that we are adding stripe to the signing up part. Should we rethink this?

- Right now event registration closes when the event starts because `registration_deadline` is being set equal to `starts_at`. Do you want organizers to be able to set a separate registration-close date/time, or should signup always close exactly at event start?
	Sign up should not be allowed after the organizer or admin, clicks start event or I guess generate schedules because after the schedules are generated, this will only include all checked in attendees and so this should stop any more attendees from signing up since they won't be in the schedules

- For `Event ends automatically the next morning`, the app currently stores `starts_at` in UTC only and does not persist a separate event timezone or run an existing background job for auto-completion. Do you want me to add an `event_timezone` field and use the creator-selected timezone for the next-calendar-morning cutoff, or should I instead treat the event creator's browser local timezone at create/edit time as the authoritative timezone and persist that?
	Persist the time zone that the event was created in by the event organizer and use that

- When the checked-in attendee count is odd, should the schedule generator always assign one break each round, or is there another pairing rule you want enforced?
	The matching algorithm with the schedule generator is already done. Please do not change any current functionality of the matching algorithm or the schedule generator thing. (matcher.py and related code)

- After a user taps `Yes` or `No` for a date, can they change that selection later, and if so until what point?
	they can change that selection until the event organizer or admin clicks end event which will in a way lock in their selections because on the UI they will now only see who they matched with and yes or no buttons will not appear for them anymore. all the coding for this has already been done, so don't worry about coding this. You can look into the code in the backend and front end if you want to confirm these things.

- When and how should matches be revealed to attendees and event organizers after the event ends?
	for attendees on the specific event card that they were participating in, their my schedule accordion will now display every day that they matched with instead of showing the date and yes or no buttons. For event, organizers and admin, they will not be able to see any matches. All of this has already been coded and accounted for. You can check the code in the backend and front end to confirm these things if you feel the need to

- Can an event organizer also participate as a normal attendee in the same event, or should organizer accounts be excluded from attendee scheduling by default?
	event organizers and Admins can attend any event. This has already been coded for and accounted for in the flow of the UI and backend. If you want to confirm this, you can look in the backend and front end code, but that is up to you

- What exact fee/pricing model should the app enforce for event creation and attendee registration? `CONTEXT.txt` mentions Stripe onboarding and per-person payment, but not the business rules.
	It costs event organizers no money to create an event. It only requires them to set up their striped connect thing to connect to my account. The event organizer will set the price per person for the event that they are creating, and the attendee will pay that exact amount with no fees. The event organizer will absorb the stripe processing fee plus my platform fee. the total of these two things combined for their first two events that they create will $1 + 5% of each ticket bought. After their first two events, it will be $1.5 + 8%. all of this has already been coded and accounted for, but you can double check my code in the backend and front end and leave any notes below this response to your question indented.
	- Follow up: I confirmed normal attendee signup uses Stripe Checkout for paid events, but the organizer/admin waitlist-promotion path currently skips payment and registers the user directly. Should that path also require payment for paid events?

- If I later need to make UX decisions that are not explicitly documented, do you want those decisions optimized first for mobile, desktop, or parity across both?
	parody across both but prioritize mobile

- The current sign-up form allows `18+`, but `PrivacyPolicy.tsx` says the service is only for ages `20-40`. Which eligibility rule should the real flow use?
	please update the privacy policy, 18+ is correct

- The current completed-event attendee UI reveals mutual matches immediately inside `MySchedule`, but the backend `/events/:id/my-matches` route blocks attendee match access until `24 hours after event completion`. Which match-reveal flow is intended?
	reveal matches when the event is completed. the 24hr thing you saw may be old code? can you check if it is ever used and delete it if not

- The backend still accepts speed-date selection submissions for up to `24 hours` after an event is completed, but the current attendee UI hides the `Yes/No` buttons and only shows results once the event is marked `Completed`. Should attendees still be able to change/save selections after the organizer ends the event?
	no, they should only be able to change their selections while the event is not complete. are there any cases where the 24hr rule would block things/functionality? respond below this indented please
	- Follow up: I checked the current main attendee selection route, and it already blocks submissions unless the event status is `In Progress`, so I do not see the old 24-hour rule affecting the live web flow there. Do you want me to treat any remaining 24-hour references/tests/scripts as stale cleanup the next time I touch that area?

- The backend allows an admin to delete even `In Progress` or `Completed` events, but the current UI disables delete for started/completed events for everyone. Should admins have a visible override in the UI?
	no, some of that code may be stale. whatever the UI has is what we are used to. 

- For `Same 2 people wont date more than once (across events)`, what exact behavior do you want once a pair has previously dated: should those pairs be completely excluded from future schedules by default, controlled by an event-level organizer toggle, or only shown as a warning?
	in matcher.py I will allow you to change this file, even though I usually say you cannot. in find all potential dates, when we search in all opposite gender list make sure we always filter out those who the current attendee has been on a speed date with before. Only change the lines when we are setting the compatible dates variable and add a filter to that list comprehension

- For `Event ends automatically the next morning`, what exact cutoff should mark an event complete: a fixed local time such as `8:00 AM`, a certain number of hours after `starts_at`, or the next calendar morning in the event’s timezone?
	Make it so that the next calendar morning of the event's start date should mark the event complete, which will intern show all attendees their mutual matches, and things like that. You can also remove the end button on the event controls for event, organizers, and admin since this will be automatically done now.
