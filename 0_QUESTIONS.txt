1. When the checked-in attendee count is odd, should the schedule generator always assign one break each round, or is there another pairing rule you want enforced?
2. After a user taps `Yes` or `No` for a date, can they change that selection later, and if so until what point?
3. When and how should matches be revealed to attendees and event organizers after the event ends?
4. Can an event organizer also participate as a normal attendee in the same event, or should organizer accounts be excluded from attendee scheduling by default?
5. What exact fee/pricing model should the app enforce for event creation and attendee registration? `CONTEXT.txt` mentions Stripe onboarding and per-person payment, but not the business rules.
6. If I later need to make UX decisions that are not explicitly documented, do you want those decisions optimized first for mobile, desktop, or parity across both?
7. The current sign-up form allows `18+`, but `PrivacyPolicy.tsx` says the service is only for ages `20-40`. Which eligibility rule should the real flow use?
8. The current completed-event attendee UI reveals mutual matches immediately inside `MySchedule`, but the backend `/events/:id/my-matches` route blocks attendee match access until `24 hours after event completion`. Which match-reveal flow is intended?
9. The backend still accepts speed-date selection submissions for up to `24 hours` after an event is completed, but the current attendee UI hides the `Yes/No` buttons and only shows results once the event is marked `Completed`. Should attendees still be able to change/save selections after the organizer ends the event?
10. The backend allows an admin to delete even `In Progress` or `Completed` events, but the current UI disables delete for started/completed events for everyone. Should admins have a visible override in the UI?
